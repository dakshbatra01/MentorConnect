# Example env variables for the backend (do NOT commit real secrets)
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/mentorconnect?retryWrites=true&w=majority"
JWT_SECRET="replace-with-a-long-random-secret"
REFRESH_SECRET="replace-with-another-long-random-secret"
CORS_ORIGIN="http://localhost:5173"
PORT=4000
NODE_ENV=development
